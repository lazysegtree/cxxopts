
# cmake
- `cmake -S <dir>` - Specifies the project root directory
- cmake is just a configuring tool, it will generate the Makefile based on the environment
- `CMakeLists.txt` - the root file
# make
- `make` build default runs `default_target` in our project
- `.PHONY` tell the target isn't an output file.

# gcc/g++
- 